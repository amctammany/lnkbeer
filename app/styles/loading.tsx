import { AppBarLayout } from "@/components/AppBarLayout";

export default function Loading() {
  return (
    <AppBarLayout title="Styles">
      <div>Loading Styles...</div>
    </AppBarLayout>
  );
}
